export default function GameInfo ({name, description}) {
    return (
        <div>
            <h2>Lista de melhores pontuações do Jogo {name}</h2>
            <p>{description}</p>
        </div>
    )
}
